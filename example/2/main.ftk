const::char GLOBAL = 'G';

int fl() {
    const::int x = 14;
    sendln!(x);
    return 0;
}

pub int main() {
    let::int itz = 14;
    var::str y = "hi";
    const::char f = 'f';
    
    sendln!(itz);
    sendln!(y);
    sendln!(f);
    sendln!(GLOBAL);
    
    y = "hello";
    sendln!(y);
    
    fl();

    return 0;
}